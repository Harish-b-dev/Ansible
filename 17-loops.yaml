- name: demo on loops
  hosts: local
  connection: local
  become: Yes
  tasks:
    - name: printing loops
      ansible.builtin.package:
        name: "{{item}}"
        service: present
      loop:
      - nginx
      - mysql
      - zip