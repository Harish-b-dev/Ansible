- name: "Shell vs command module"
  hosts: local
  connection: local
  tasks:
    - name: users data
      ansible.builtin.shell: cat /etc/passwd | grep root
      register: user_output
    
    - name: printing user data
      ansible.builtin.debug:
        msg: "{{user_output}}"